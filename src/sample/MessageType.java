package sample;

public enum MessageType {
    USER_REGISTERED, USER_NOT_REGISTERED, USER_NOT_REGISTERED_DUPLICATE,USER_NOT_REGISTERED_WRONG_FORMAT, REGISTER, LOGIN,
    LOGIN_SUCCESSFULL, LOGIN_FAILED
}
